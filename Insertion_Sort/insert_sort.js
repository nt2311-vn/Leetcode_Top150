class Pair {
	/**
	 * @param {number} key - The key to be stored in the pair
	 * @param {numbeer} value - The value to be stored in the pair
	 */
	constructor(key, value) {
		this.key = key;
		this.value = value;
	}
}

class Solution {
	/**
	 * @param {Pair[]} pairs
	 * @returns {Pair[][]}
	 */
	insertionSort(pairs) {}
}
