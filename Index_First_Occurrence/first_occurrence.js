/**
 * @param {string} haystack - larger string
 * @param {string} needle - smaller string
 * @returns {number} returns the first index occur
 */
var strStr = function (haystack, needle) {
	return haystack.indexOf(needle);
};
